// // import { put, call, select } from 'redux-saga/effects'
// import { fakeAccountLogin } from '../services/api.';

// // test('adds 1 + 2 to equal 3', () => {
// //     expect(call(fakeAccountLogin, { username: "admin", password: "888888" })).toBe({ status: "ok", type: "account", currentAuthority: "admin" });
// // });
// // test('checkout Saga test',
// //     call(fakeAccountLogin, { username: "admin", password: "888888" }), 'must call api.buyProducts(cart)'

// // )

// // describe('test convert nested menu to flat menu', () => {
// //     it('zzzz', () => {
// //         expect(call(fakeAccountLogin, { username: "admin", password: "888888" })).toEqual(
// //             { status: "ok", type: "account", currentAuthority: "admin" });
// //     })
// // });
// describe('test convert nested menu to flat menu', () => {
//   it('simple menu', () => {
//     expect(fakeAccountLogin).toEqual(3);
//   });
// });
